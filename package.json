{
  "name": "alajs",
  "version": "1.0.0",
  "description": "Copy-Paste content attribution. Scale your link building with content attribution.",
  "main": "src/index.js",
  "repository": "https://github.com/marcocianetti/alajs",
  "author": "Marco Cianetti",
  "license": "MIT",
  "scripts": {
    "minify": "uglifyjs src/index.js --compress --mangle --warn --output lib/index.min.js",
    "prod": "npm run version:bump --silent && rm -f lib/index.min.js && npm run minify --silent",
    "version:bump": "npm version patch --no-git-tag-version --silent"
  },
  "devDependencies": {
    "uglify": "^0.1.5"
  }
}
